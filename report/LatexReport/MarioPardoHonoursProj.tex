\documentclass[12pt]{report}

%% ============================================================
%% PACKAGES
%% ============================================================
\usepackage[top=0.5in, bottom=0.5in, left=1.25in, right=1in, headheight=15pt, headsep=16pt]{geometry}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{gensymb}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{chngcntr}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{float}
\usepackage{svg}
\usepackage{emptypage}   % suppress headers on blank pages
\usepackage{needspace}   % prevent orphaned chapter headings in TOC

%% ============================================================
%% KEEP TOC CHAPTER ENTRIES WITH THEIR SECTIONS
%% ============================================================
\makeatletter
\let\orig@l@chapter\l@chapter
\renewcommand{\l@chapter}[2]{%
    \needspace{8\baselineskip}%
    \orig@l@chapter{#1}{#2}%
}
\makeatother

%% ============================================================
%% SPACING & TYPOGRAPHY
%% ============================================================
\onehalfspacing
\setlength{\parskip}{6pt}

%% ============================================================
%% FIGURE NUMBERING (SEQUENTIAL)
%% ============================================================
\counterwithout{figure}{chapter}

%% ============================================================
%% PAGE BREAK QUALITY
%% ============================================================
\raggedbottom                 % don't stretch pages to fill — let them end naturally
\widowpenalty=9999            % strongly discourage widows (lone last line at top of page)
\clubpenalty=9999             % strongly discourage orphans (lone first line at bottom)
\usepackage{etoolbox}
\preto{\section}{\needspace{5\baselineskip}}  % never start a section near the bottom of a page

%% ============================================================
%% HYPERREF
%% ============================================================
\hypersetup{
    colorlinks = true,
    linkcolor  = black,
    citecolor  = black,
    urlcolor   = blue,
    pdftitle   = {SLAM From Scratch},
    pdfauthor  = {Mario Pardo},
}

%% ============================================================
%% CODE LISTINGS
%% ============================================================
\lstset{
    basicstyle   = \small\ttfamily,
    breaklines   = true,
    frame        = single,
    captionpos   = b,
    numbers      = left,
    numberstyle  = \tiny\color{gray},
    tabsize      = 4,
}

%% ============================================================
%% HEADERS / FOOTERS
%% ============================================================
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{\small\leftmark}
\fancyhead[R]{\small\thepage}
\fancypagestyle{plain}{%   used on chapter-opening pages
    \fancyhf{}
    \renewcommand{\headrulewidth}{0pt}
    \fancyfoot[C]{\thepage}
}

%% ============================================================
%% CHAPTER TITLE FORMAT
%% ============================================================
\titleformat{\chapter}[block]
    {\normalfont\LARGE\bfseries}
    {\thechapter.}{0.8em}{}
\titlespacing*{\chapter}{0pt}{24pt}{16pt}

%% ============================================================
%% BEGIN DOCUMENT
%% ============================================================
\begin{document}

% ============================================================
%  TITLE PAGE
% ============================================================
\begin{titlepage}
    \centering
    \vspace*{1.5cm}

    {\LARGE \textbf{Carleton University}\\[0.4cm]}
    {\large School of Computer Science\\[0.3cm]}
    {\large COMP 4905 --- Honours Project\\[2.5cm]}

    \rule{\textwidth}{1.5pt}\\[0.6cm]
    {\Huge \textbf{SLAM From Scratch}}\\[0.4cm]
    \rule{\textwidth}{1.5pt}\\[2.5cm]

    \begin{minipage}{0.45\textwidth}
        \centering
        {\large \textbf{Submitted by:}\\[0.3cm]}
        {\large Mario Pardo}
    \end{minipage}
    \hfill
    \begin{minipage}{0.45\textwidth}
        \centering
        {\large \textbf{Supervisor:}\\[0.3cm]}
        {\large Mark Lanthier}
    \end{minipage}

    \vfill
    {\large April 2026}
\end{titlepage}

% ============================================================
%  FRONT MATTER  (Roman numerals)
% ============================================================
\pagenumbering{roman}
\setcounter{page}{1}

% ---- Abstract -----------------------------------------------
\chapter*{Abstract}

Humans are able to navigate and understand new environments quickly and intuitively,
combining sensory inputs with spatial memory to create an understanding of their environment
and their location within it.
Replicating this in machines is a complicated task encompassing various areas of research and
is a key challenge in developing autonomous robots and vehicles.
Simultaneous Localization and Mapping (SLAM) is a class of algorithms dedicated to just this task,
allowing systems to understand the environment in which they operate and where they are within
it --- especially critical in scenarios where external positioning sources, such as GPS, are unavailable.
Pose Graph SLAM is a popular implementation of SLAM used by most modern state of the art systems, and 
that provide a computationally efficient, yet effective, solution to the localization and mapping
problem. 

This project presents a C++ implementation of a Pose Graph SLAM system developed from first
principles.
The resulting system demonstrates reliable convergence in pose-graph optimization, validating
that a ground-up implementation can manage the underlying mathematical complexity and
high-performance requirements of modern robotics.

\newpage

% ---- Acknowledgments ----------------------------------------
\chapter*{Acknowledgments}

I would like to express my sincere gratitude to my supervisor, Professor Mark Lanthier, for his
guidance, feedback, and support throughout this project.

I am also deeply grateful to my family for their unwavering encouragement and support during my
studies.

Finally, I would like to acknowledge Professor Cyrill Stachniss of the University of Bonn, whose
publicly available lecture series on robotics and SLAM were invaluable in building my understanding
of the field and informed much of the implementation presented in this report.

\newpage

% ---- Table of Contents --------------------------------------
{\pagestyle{plain}\tableofcontents}
\newpage

% ---- List of Figures ----------------------------------------
\listoffigures
\newpage


% ============================================================
%  MAIN BODY  (Arabic numerals, starting from 1)
% ============================================================
\pagenumbering{arabic}
\setcounter{page}{1}

% ============================================================
\chapter{Introduction}
% ============================================================

Robotic systems sense the world and themselves through a variety of sensors, such as wheel encoders,
gyroscopes, and lidar sensors.
The two former are combined to create \textit{odometry} data, estimating the change in position and
orientation (pose), while the latter uses laser reflections to sense the geometry of the world at a
distance.
In real-world settings, odometry data carries noise from sensor readings and misalignments between
these readings and the motion they imply --- for example, wheels slipping over bumps.
This causes \textit{drift}: an error in the calculated pose compared to the actual pose.
As pose estimates build on top of one another, this drift accumulates quickly, leading to worsening
estimates and conflicts with incoming sensor data --- ultimately producing catastrophic errors in
localization and mapping.

To illustrate. Assume our robot is at $(2\text{ m},\; 0\text{ m})$, but our current estimates think that the 
robot is at $(3\text{ m},\; 1\text{ m})$.
If our lidar tells us that there is a wall 3m ahead, then we will register this wall to be at $(6\text{ m},\; 1\text{ m})$, 
when in reality our wall is at $(5\text{ m},\; 0\text{ m})$.\newline
Frame by frame, these error quickly compound leading to highly innacurate trajectory and mapping estimations. 

A Graph-Based SLAM system constructs a graph of poses and movements, and when it detects a
\textit{loop closure} --- a location previously visited --- it uses this new observation to update all
previous pose estimates, ``snapping'' both trajectory and map into a corrected version.

The intention of this project was to open this algorithmic black box and gain rigorous experience
studying, understanding, implementing, debugging, and testing a complex system from first principles.
Developing a SLAM algorithm tested knowledge in mathematics --- specifically linear algebra and
statistics --- and its application to a complex, multi-step, real-world system written in
high-performance C++.

% ============================================================
\chapter{System Architecture}
% ============================================================

A SLAM system starts with sensor inputs and, over time, accumulates information to locate itself and
map its surroundings.
It is composed of a chain of subsystems of varying complexity, each depending on the step before it
to produce meaningful results.

The system can be broken down into 4 main stages, with the flow shown in the diagram below. 
\begin{enumerate}
    \item \textbf{Sensor Integration}: Collect sensor data and process into usable odometry data and point clouds
    \item \textbf{Transformation Estimation:} Iterative Closest Point (ICP) to estimate robot motion
and map surrounding environment
    \item \textbf{Pose Graph Construction}: Keep track of trajectory and mapping data through the construction of a Pose Graph
    \item \textbf{Loop Closures and Pose Graph Optimization}: If robot has recognized a familiar pose, try to correct error in pose graph
\end{enumerate}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.72\textwidth]{../images/PG-SLAM_Architecture.png}
    \caption{Visualization of a the data flow and main components of the SLAM System.}
    \label{fig:slam_architecture}
    \small{Sensors are used to obtain odometry and point cloud data, which is used by ICP for transform
    estimation. As the robot traverses the pose graph is continuously updated, and if succesfull loop clousures are found,
    pose graph optimization is run to reduce error in the graph}
\end{figure}

\newpage

\section{Technology Stack}

\textbf{C++ --- Core Algorithm.}
C++ continues to be the language of choice for high-performance code.
Writing the backend in C++ provided an excellent opportunity to make use of its powerful features and
meet the intense performance requirements of robotics software.
Key libraries used:
\begin{itemize}
    \item \textbf{Eigen} --- linear algebra.
    \item \textbf{GTSAM} --- pose graph optimization.
\end{itemize}

\textbf{Webots --- Robotics Simulation.}
Webots is a simple yet powerful open-source robotics simulation platform.
It provides ready-made sandboxes and robot models, such as the TurtleBot Burger 3 used in this
project, with the necessary setup for motion and sensor usage.
Using these ready-made tools allowed focus on the core SLAM implementation and allows for easy
scaling of the project.

\textbf{Python --- Visualization and Communication.}
Python was used on both sides of visualization: interfacing with Webots for robot simulation, and on
the backend for visualizing trajectories, occupancy grids, and system outputs.
It was selected for its ease of use and wide library support.

\textbf{ZeroMQ --- Communication Protocol.}
ZeroMQ was selected for communication between the front end, backend, and visualization tools.
It implements the Publish/Subscribe architecture, which is well suited to a use case where systems
must communicate consistently in both directions without requiring full synchronization.
The name reflects its need for no broker or central server, making it resilient to any single endpoint
failing, and it is straightforward to implement from both C++ and Python.

% ============================================================
\chapter{World Setup and Sensor Integration}
% ============================================================

Various sensors in the robot acquire data at each timestep.
This project makes use of:
\begin{itemize}
    \item \textbf{Wheel encoders} --- measure the rotation of each wheel, processed into odometry data
          that estimates the robot's displacement.
    \item \textbf{Lidar} --- a $360^\circ$ scan of the environment using laser light; the reflection of
          each projected beam records the distance to the nearest surface at a given angle.
\end{itemize}

This data is gathered in Webots and the raw values are forwarded to the C++ backend for processing.

\section{Arena and Robot Setup}

The first step was to set up the testing arena and robot.
A simple square arena was created, and was populated with various and assymetric landmarks.
These landmarks are important for ICP (Chapter~\ref{chap:icp}) to work with, as well as for easy diagnostics 
though visualization tools. 
The premade TurtleBot Burger 3 was selected, already equipped with all the necessary actuators and sensors
for our project. 
A simple differential drive controller was created and tested to confirm reliable movement and control of the robot.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{../images/Experiments/RegularMap/RegularMap_MapImg.png}
    \caption{Webots arena used for Development}
    \title{fig:image_regular_map}
    \small{Square rectangle arena with checkerboard flooring, alongside a yellow ball and various white walls as landmarks}
\end{figure}

\section{Odometry}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{../images/DifferentialDrive.png}
    \caption{Diagram of differential drive}
    \small\textit{Taken from: Dunbar and Esposito \cite{dunbar2005swarm}.}
    \label{fig:diff_drive}
\end{figure}

A differential drive robot steers by varying the speed of its two wheels independently.
We 
At each timestep, wheel encoders report the angular displacement of each wheel, $\Delta\phi_l$ and
$\Delta\phi_r$ (in radians) for each wheel. \newline
Multiplying by the wheel radius $r$ converts these to linear distances:
\begin{equation}
    d_l = r\,\Delta\phi_l, \qquad d_r = r\,\Delta\phi_r
\end{equation}

The incremental displacement and heading change of the robot centre are then:
\begin{align}
    \Delta s      &= \frac{d_r + d_l}{2} \\[4pt]
    \Delta\theta  &= \frac{d_r - d_l}{w}
\end{align}
where $w$ is the wheelbase.
The pose $(x,\,y,\,\theta)$ is updated differently depending on whether the motion is
effectively straight or curved.

\textbf{Straight motion} ($|\Delta\theta| < \varepsilon$).
When the heading change is negligible, the robot moves in a straight line along its current
heading:
\begin{align}
    x'      &= x + \Delta s\cos\theta \\
    y'      &= y + \Delta s\sin\theta
\end{align}
A threshold $\varepsilon$ guards against division-by-zero and numerical instability in the arc
formula below.

\newpage

\textbf{Arc motion} ($|\Delta\theta| \geq \varepsilon$).
When the robot turns, it traces a circular arc of radius $R = \Delta s / \Delta\theta$ around an 
Instananeous Center of Curvature (ICC), shown in Figure \ref{fig:diff_drive_rotation}. 
The exact displacement for constant-curvature motion is obtained by the following.
\begin{align}
    x'      &= x + R\bigl(\sin(\theta + \Delta\theta) - \sin\theta\bigr) \\
    y'      &= y + R\bigl(\cos\theta - \cos(\theta + \Delta\theta)\bigr)
\end{align}
Note that sin and cos are used for x and y respectively, when normally this is reversed. 
This is due to the ICC being at 90\degree to the robot. 

In both cases the heading is updated identically:
\begin{equation}
    \theta' = \theta + \Delta\theta
\end{equation}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{../images/DiffDriveRotation.png}
    \caption{Diagram of rotation with Differential Drive}
    \small\textit{Taken from: Mishra et al.\ \cite{mishra2020kinematics}.}
    \label{fig:diff_drive_rotation}
\end{figure}

\textbf{Noise.}
In a real-world setting, wheel sensors are not perfect.
They can record movement that does not result in actual robot displacement --- such as wheel slip on
wet terrain --- or introduce sensor measurement noise.
Modelling this noise is important, as correcting for it is a core responsibility of the SLAM system.

Odometry provides the first reference point for the SLAM algorithm.
Later on during ICP (Chapter~\ref{chap:icp}), odometry trajectory is used as a starting off point for 

Wheel sensors and therefore odometry data can be quite noisy, which is why point clouds, which are much less prone to noise, 
are used for calculating trajectory using ICP. 


\newpage

\section{Point Cloud}
Our LiDAR sensor uses laser pulses to create a 2d map of our surroundings. This gives us a consistent and precise
source our data of our surroundings, which we process with the methods shown below. 
The lidar emits $N$ beams uniformly spread over a $360^\circ$ field of view.
Each beam $i$ returns a range $r_i$ --- the distance to the nearest surface along angle
$\alpha_i = \frac{2\pi i}{N}$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{../images/LidarDiagram.jpeg}
    \caption{Diagram of Lidar Points from robot POV}
    \small{Robot shown in green, blue dotted line representing a single point cloud ray, as well
    as the radius of the 360\degree lidar, with a landmark shown in red.}
    \label{fig:lidar_diagram}
\end{figure}

\textbf{Robot frame.}
Using basic trigonometry, each beam's return is converted to a 2-D point relative to the robot:
\begin{equation}
    p_i^{\,\text{robot}} =
    \begin{pmatrix} r_i \cos\alpha_i \\ r_i \sin\alpha_i \end{pmatrix}
\end{equation}
This gives the point cloud in the robot's own coordinate frame, centred at the robot's current
position with the robot's heading as the forward axis.

\textbf{World frame projection.}
With the current robot pose $(x,\, y,\, \theta)$, each point then is transformed into the fixed world
frame by applying a 2-D rigid-body transform:
\begin{equation}
    p_i^{\,\text{world}} =
    \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \phantom{-}\cos\theta \end{pmatrix}
    p_i^{\,\text{robot}}
    +
    \begin{pmatrix} x \\ y \end{pmatrix}
\end{equation}
This places each observed surface point on the global map at the location the robot estimates it
to occupy.
This transformed point cloud data is used by ICP (Chapter~\ref{chap:icp}) to estimate the robot's trajectory, 
as well as by the occupancy grid to map the environment. 




\newpage

\section{Webots Lidar Noise During Rotation}
\label{sec:webots_bug}

Even after introducing rotation gating for localization (described in
Section~\ref{sec:icp_rotation}), significant map smear persisted.
After considerable troubleshooting, the source was identified as a Webots simulator issue: while
the robot is rotating, the lidar scan twitches back and forth and only snaps back into place once
rotation finishes, producing very noisy and incorrect lidar data during the rotation.
The fix was to discard all lidar data collected while the robot is rotating, using only readings when the 
robot is undergoing no rotation, where lidar readings are accurate

The figure below shows a working odometry trajectory combined with a single instance of lidar data. 
Due to it being a single instance, the mentioned "smear" is not shown

\begin{figure}[H]
    \centering
    \includegraphics[width=0.72\textwidth]{../images/VisualizeLidarAndTrajTest.png}
    \caption{Initial visualizations showing odometry trajectory estimation , and basic cloud point recognition}
    \label{fig:first_traj_lidar_viz}
\end{figure}

% ============================================================
\chapter{Occupancy Grid}
% ============================================================

To accomplish the mapping portion of SLAM, an occupancy grid represents the map by storing the
likelihood that each cell is occupied, encoded using log-odds:

\begin{equation}
    \ell(m_k) = \log \frac{P(m_k \mid z_{1:t},\; x_{1:t})}{1 - P(m_k \mid z_{1:t},\; x_{1:t})}
\end{equation}

where $m_k$ is the $k$-th cell, $z_{1:t}$ is the history of sensor readings, and $x_{1:t}$ is the
history of poses.
As the robot receives lidar returns, the likelihood of occupied cells along each beam is increased,
and cells along the free path are decreased.

The occupancy grid is only updated on new keyframes (Chapter 6) to ensure readings come from stable
and trusted data. 

To find out which cells are occupied and not, we use the \textbf{BresenhamLine Algorithm} to "trace" a line
through our grid.
For each ray, we draw a line. We take the endpoint of our ray to be occupied, and anything before it to be free. 
This is then used to update the occupation probability of each cell. 
The figure below shows an example of four separate rays being casted, and the resulting occupied (grey) 
and unnocupied (white) area

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{../images/RayCastOccGrid.png}
    \caption{Ray Casting and Occupancy}
    \small\textit{Taken from: Rodr\'iguez-Losada et al.\ \cite{rodriguez2010grid}.}
    \label{fig:raycast_occ_diagram}
\end{figure}

% ============================================================
\chapter{ICP: Scan Matching}
\label{chap:icp}
% ============================================================

Iterative Closest Point (ICP) is one of the most crucial components of the SLAM system.
It takes two point cloud readings --- a previous scan and a current scan --- and estimates how the
robot has moved between them.

The algorithm, at a high level, proceeds as follows: \newline

\noindent\textbf{alignPointClouds}$\left(\texttt{sourcePointCloud},\; \texttt{targetPointCloud},\; \texttt{odometryInitialGuess}\right)$
\begin{enumerate}
    \item Transform the source point cloud by the initial guess provided by odometry.
    \item \textbf{Find correspondences}: identify which points in the source and target clouds
          represent the same surface.
    \item Using these correspondence pairs, \textbf{estimate the transformation} between the two
          clouds.
    \item Update the current estimate with the refined transformation.
    \item If the change in error falls below a threshold, ICP has \textbf{converged}.
\end{enumerate}

\section{Correspondence Matching}
To estimate the transformation between point clouds, we must first find pairs of points (source, target) in the two 
point clouds, which we deem to be the same place on the surface, just from different point of view. 
This change in the point of view is the transformation we wish to compute. 
\newline
The most simple method of corrßespondence matching was used, \textbf{Point to Point}.
\newline
The logic goes as follows: 
\newline for each point in the source cloud, the closest
point in the target cloud is found and the pair is considered a correspondence.

A key hyperparameter is \texttt{max\_distance}, the maximum distance at which two points can be
paired.
This is a trade-off between precision (smaller distance yields more accurate transformations) and
generality (too small a distance results in too few matches).


\section{Estimating the Transform}

Given the set of correspondences, the transformation is estimated using the Singular Value Decomposition (SVD) method.

Let $\mu_s$ and $\mu_t$ denote the centroids of the source and target point sets respectively. Let $W = \sum_i (p_i - \mu_s)(q_i - \mu_t)^\top$ be their cross-covariance. Taking their outer product tells us how each pair of axes from each group correlates. 

The SVD of $W$ is given by $W = U D V^\top$. We can interpret $V$ as the directions coming \textbf{into} the transformation, $U$ as the transformed \textbf{output} direction vectors for those same inputted directions, and $D$ as the scaling for each direction. 

From this, we get the optimal rotation:
\begin{equation}
    R = V U^\top
\end{equation}

And then to get the full transformation, we find the translation from the rotated source centroid to the output centroid:
\begin{equation}
    t = \mu_t - R\,\mu_s
\end{equation}

Since a straightforward analytical solution is known, implementation was a matter of correctly
converting these equations into code, verified by unit tests.

\section{Convergence}

The error between successive transformation estimates is computed.
If it falls below a threshold, ICP is considered converged.
This required hyperparameter tuning: errors were printed across many runs, the best 10\% were
identified, and the threshold was iteratively narrowed to one that gave consistent yet precise
convergences.


\section{Handling Rotation}
\label{sec:icp_rotation}

Large rotations cause point clouds to change drastically from little robot movement, so ICP
inherently struggles to find good transformations during rapid rotation.
To address this, \textit{gating} was introduced: if the robot's angular velocity exceeds a threshold,
the odometry-estimated transformation is used instead of running ICP.
This is a simple and effective fix, though a production system would use more sophisticated sensor
fusion such as an Extended Kalman Filter to intelligently weight sensor sources when any one cannot be
fully relied upon.

Expanding the lidar field of view from $90^\circ$ to $360^\circ$ was a major breakthrough for ICP
accuracy and consistency.
With a $90^\circ$ FOV, even small rotations shift a large fraction of the visible point cloud,
making correspondence matching difficult.
With a full $360^\circ$ view, surfaces all around the robot remain visible throughout rotation,
providing ICP with stable correspondences.

\section{ICP 180-Degree Inversion Bug}

Early in testing, ICP produced transformations at $180^\circ$ from the actual movement.
The initial patch manually rotated the output by $180^\circ$, which worked but was clearly a symptom
of a deeper issue.

The root cause was a source/target mismatch.
Consider a robot at $(0,0)$ with a wall at $(0,2)$.
From the robot's frame, the wall is 2~m ahead.
After the robot moves forward 1~m to $(0,1)$, the wall appears 1~m ahead: from the robot's frame the
wall has moved by $(0,-1)$.
This is geometrically correct from the robot's point of view, but produces an inverted transformation.

To correct this, the source and target must be swapped.
If the source point is at relative position $(0,1)$ and the target at $(0,2)$, the transformation
found is $(0,+1)$, which correctly describes the robot's forward motion.




% ============================================================
\chapter{Pose Graph Construction}
% ============================================================

The pose graph is the record of where the robot has been and how the world has appeared at each
timestep. 
\textbf{Graph Nodes} contain:
\begin{itemize}
    \item \textbf{Pose} --- estimated position $(x, y)$ and heading angle $\theta$.
    \item \textbf{Point cloud} --- how the world appeared at this pose.
    \item \textbf{ID and Timestamp} --- for identification and ordering.
\end{itemize}


\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{../images/PoseGraphDiagram.jpg}
    \caption{Diagram of Pose Graph}
    \small{Figure shows the relation between the information stored in the pose graph,
to the ground truth information about the environment, and the robot's trajectory.
    \newline
    \textit{Taken from: Pfingsthorn \cite{pose_graph_thesis}.}}
    \label{fig:pose_graph}
\end{figure}


\textbf{Nodes} (keyframes) are added only once the robot has moved a minimum distance $d$ or rotated a minimum
angle $\delta\theta$, tuned through trial and error.
This is a trade-off: too many nodes overwhelm the optimizer; too few forces ICP to find
transformations across large gaps.
For a square world nearly 2~m diagonally across, a keyframe step of 15~cm, and angle change of 0.2radians (~12\degree) was large enough not to
overwhelm the optimizer while small enough for ICP to find accurate transformations.

The visualization tool was updated to display graph nodes and their ICP-estimated edges.

% ============================================================
\chapter{Loop Closure Detection}
% ============================================================

As the robot traverses the world and adds keyframes, the current position is compared with old graph
nodes to detect whether a previously visited location has been returned to.
An exact match is not required; being within a sufficient proximity is enough.
If a candidate is found, ICP is run against the old node's point cloud to confirm the loop closure
and obtain a precise relative pose estimate.
A special \textit{loop closure edge} is then added between the current node and the matched old node.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{../../Pictures/ProgressPics/LoopClosure}
    \caption{Visualization of detected loop closures edges (cyan) between pink nodes.}
    \label{fig:loop_closure}
\end{figure}

If no good match is found, or ICP does not converge on a consistent transformation, the robot
continues accumulating keyframes as normal.

While loop closures reuse much of the ICP infrastructure, they are a crucial step: they determine
when and how the graph is optimized, and therefore when accumulated error is corrected.

Hyperparameter tuning was required, notably for correspondence distance and error epsilon.
The goal is accurate loop closure edges that are also general enough to reliably trigger when
revisiting a location.
Too selective, and loop closures never trigger; too permissive, and false closures corrupt the graph.

\section{Performance Tuning}

Loop closure detection requires running ICP repeatedly --- a computationally expensive process ---
so the set of candidate nodes is filtered aggressively before ICP is invoked:

\begin{itemize}
    \item \textbf{Recency filter.}
          Useful loop closures only involve nodes from well before the current position.
          Only nodes at least $N = 10$ keyframes back are considered.

    \item \textbf{Proximity filter.}
          A loop closure requires physical proximity.
          The 6 closest candidate nodes by Euclidean distance are retained before running ICP.

    \item \textbf{Temporal gating.}
          Once a loop closure has been added, subsequent closures in the same vicinity provide
          diminishing benefit and can challenge the optimizer.
          Loop closure attempts are therefore gated to a minimum of 3 keyframes after the last
          successful closure, providing significant computational savings without limiting
          system effectiveness.
\end{itemize}

% ============================================================
\chapter{Pose Graph Optimization}
% ============================================================

With a loop closure edge added, the graph forms a full loop and is ready to be optimized.
Optimization finds the arrangement of node poses that minimizes the total error across all edges.
This step is what "snaps" the map in place, through the reduction of acculumated drift.

\section{Problem Formulation}

Let $\mathbf{x} = \{x_1, x_2, \ldots, x_n\}$ denote the state: the set of node poses.
Let $\hat{z}_{ij}$ denote the \textit{measured relative pose} between nodes $i$ and $j$, as
estimated by ICP.
Let $z_{ij}(\mathbf{x})$ denote the \textit{predicted relative pose}, computed from the difference
in stored node coordinates.

The error for edge $(i,j)$ is:
\begin{equation}
    e_{ij}(\mathbf{x}) = z_{ij}(\mathbf{x}) - \hat{z}_{ij}
    \label{eq:error}
\end{equation}

Each edge carries an information matrix $\Omega_{ij}$, representing confidence in the measurement
, derived from how much the ICP result differed from the odometry prediction.
The weighted squared error per edge is:
\begin{equation}
    \varepsilon_{ij}(\mathbf{x})
        = e_{ij}(\mathbf{x})^\top\, \Omega_{ij}\, e_{ij}(\mathbf{x})
\end{equation}

Thus, to minimize error, the optimization goal is:
\begin{equation}
    \mathbf{x}^* = \arg\min_{\mathbf{x}}\;
        \sum_{(i,j) \in \mathcal{E}} e_{ij}(\mathbf{x})^\top\, \Omega_{ij}\, e_{ij}(\mathbf{x})
    \label{eq:opt_goal}
\end{equation}

\section{Solving via Iterative Local Linearization}

This nonlinear least-squares problem is solved using iterative local linearization, upon which the
Levenberg--Marquardt optimizer provided by GTSAM builds.

\textbf{Step 1 --- Linearize around the current estimate.}
\begin{equation}
    e_{ij}(\mathbf{x} + \Delta\mathbf{x})
        \approx e_{ij}(\mathbf{x}) + J_{ij}\,\Delta\mathbf{x}
\end{equation}
where $J_{ij}$ is the Jacobian of $e_{ij}$ with respect to $\mathbf{x}$.
Linearizing transforms a curved error landscape into a locally flat one that can be solved with
linear algebra.

\textbf{Step 2 --- Expand the squared error in terms of $\Delta\mathbf{x}$.}
\begin{align}
    \varepsilon_{ij}
        &= \bigl(e_{ij} + J_{ij}\Delta\mathbf{x}\bigr)^\top
           \Omega_{ij}
           \bigl(e_{ij} + J_{ij}\Delta\mathbf{x}\bigr) \nonumber \\
        &= \underbrace{e_{ij}^\top \Omega_{ij} e_{ij}}_{C_{ij}}
         + 2\underbrace{e_{ij}^\top \Omega_{ij} J_{ij}}_{b_{ij}^\top}\Delta\mathbf{x}
         + \Delta\mathbf{x}^\top
           \underbrace{J_{ij}^\top \Omega_{ij} J_{ij}}_{H_{ij}}
           \Delta\mathbf{x}
    \label{eq:expand}
\end{align}
where $b_{ij} = J_{ij}^\top\Omega_{ij}\,e_{ij}$ encodes the gradient direction and
$H_{ij} = J_{ij}^\top\Omega_{ij}J_{ij}$ is the Gauss--Newton Hessian approximation.

\textbf{Step 3 --- Differentiate, set to zero, and solve for $\Delta\mathbf{x}$.}
The GTSAM solver handles this step internally, iterating until convergence.

\section{Implementation}

GTSAM is a well-known and well-tested pose graph optimization library.
A \texttt{FactorGraph} was constructed from the existing \texttt{PoseGraph} data structure and
passed to GTSAM's Levenberg--Marquardt optimizer.
The optimized poses were then written back into the graph, and the point clouds of each node were
reprojected at the updated pose coordinates to remove map smear accumulated from drift.

Similar to loop closure, pose graph optimization is computationally heavy.
It is therefore gated to a minimum of 10 keyframes since the last optimization run.

Initial attempts produced unexpected graph translations and, most notably, no improvement to
mapping --- smear was not removed.
This led to a thorough evaluation of the full SLAM pipeline, including implementation of
point-to-plane correspondence matching in ICP and iterative hyperparameter refinement.
The Webots lidar rotation issue (Section~\ref{sec:webots_bug}) was discovered during this
investigation, and resolving it was the key breakthrough that brought the full system together.

% ============================================================
\chapter{Results}
% ============================================================

This project produced a working and reliable SLAM implementation able to localize the robot's
trajectory and build an accurate occupancy grid map of its surroundings.

During development a single testing environment was used, featuring a variety of surface types and
lengths, and a deliberate lack of symmetry and repetition.
For final testing, six new environments were used, each designed to stress the system under
different conditions or to evaluate the impact of specific structural features such as distinct
landmarks.
All maps except the Apartment map were custom-built and fit within a $2.5 \times 2.5$~m arena.

Each test environment is presented with three outputs:
\begin{enumerate}
    \item \textbf{Occupancy grid} --- the map produced by the system.
    \item \textbf{Localization error over time} --- normalised as
          $\text{error} = \tfrac{d(\text{ICP},\,\text{ground truth})}{\text{max possible distance}}$.
    \item \textbf{Trajectory comparison} --- overlaying four traces:
          \begin{itemize}
              \item \textbf{White} --- ground truth from GPS.
                    Often hidden beneath other traces, indicating near-perfect agreement.
              \item \textbf{Blue} --- odometry-only trajectory, highlighting the drift that SLAM must correct.
              \item \textbf{Green} --- ICP-estimated trajectory.
              \item \textbf{Pink} --- optimised pose-graph nodes and trajectory.
                    Where the pink and green traces diverge, the pose-graph optimizer has corrected
                    accumulated ICP error.
          \end{itemize}
\end{enumerate}

For nearly all maps, we see a sharp dropoff in the localization error over time, this is due to the Pose Graph
Optimization doing it's job and reducing error. 

\newpage

\subsection*{Hallway Map}

A simple rectangular map composed of straight hallways --- a clean baseline environment with
unambiguous straight-line features.

\begin{figure}[H]
    \centering
    % --- Row 1: map image, trajectory, occupancy grid ---
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/HallwayMap/HallwayMap_MapImg.png}
        \caption{environment image.}
        \label{fig:hallway_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/HallwayMap/HallwayMap_traj.png}
        \caption{Trajectory comparison.}
        \label{fig:hallway_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/HallwayMap/HallwayMap_grid.png}
        \caption{Occupancy grid output.}
        \label{fig:hallway_grid}
    \end{subfigure}

    \vspace{0.8em}

    % --- Row 2: localisation error ---
    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/HallwayMap/HallwayMap_errorline.png}}}
        \caption{Localisation error over time.}
        \label{fig:hallway_error}
    \end{subfigure}

    \caption{Hallway Map results.}
    \label{fig:hallway_results}
\end{figure}

The ground-truth white line is almost entirely hidden by the pink optimised trajectory, confirming
that the system achieves near-perfect localisation accuracy on this straightforward environment.
The mapping is excellent, but has a small issue at the bottom.

\newpage
\subsection*{Hallway Map --- With Landmarks}

Landmark surfaces are added to the walls to provide additional ICP features and improve localisation accuracy.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/HallwayMap/HallwayMap_Landmark_MapImg.png}
        \caption{environment image.}
        \label{fig:hallway_lm_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/HallwayMap/HallwayMap_Landmark_traj.png}
        \caption{Trajectory comparison.}
        \label{fig:hallway_lm_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/HallwayMap/HallwayMap_Landmark_grid.png}
        \caption{Occupancy grid output.}
        \label{fig:hallway_lm_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/HallwayMap/HallwayMap_Landmark_errorline.png}}}
        \caption{Localisation error over time.}
        \label{fig:hallway_lm_error}
    \end{subfigure}

    \caption{Hallway Map (with landmarks) results.}
    \label{fig:hallway_lm_results}
\end{figure}

We see that landmarks provided a noticeable improvement in performance. From visual inspection, the tajectory is closer 
to ground truth, specifically that the green line is less visible, telling us that Pose Graph Optimization had lessworkd to do. 
Likelise for mapping, we no longer have that "artifact" at the bottom of the map.

\newpage
\subsection*{Round Hallway}

Now we test the same hallway idea, but the walls are now curved

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RoundHallway/RoundHallway_MapImg.png}
        \caption{environment image.}
        \label{fig:round_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RoundHallway/RoundHallway_traj_noloop.png}
        \caption{Trajectory comparison.}
        \label{fig:round_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RoundHallway/RoundHallway_Grid.png}
        \caption{Occupancy grid output.}
        \label{fig:round_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/RoundHallway/RoundHallway_error_overtime.png}}}
        \caption{Localisation error over time.}
        \label{fig:round_error}
    \end{subfigure}

    \caption{Round Hallway results.}
    \label{fig:round_results}
\end{figure}

This is our first negative result. We see that the round walls have a detrimental effect on our trajectory. 
Looking at the trajectory comparison, we see that the ICP trajectory is actually quite far back compared to our ground truth. While
the white ground truth line has made a full loop, ICP still believes we are a bit far back, around 1/8th of the circle back. 
Then, when a loop closure is found and the Pose Graph Optimized, it is unable to remove mcuh error at all compared to other maps. 

This result shows the clear negative effect of round surfaces on ICP

We must also analyze that while trajectory is affected, mapping is not. Due to the radial symmetry of the circular path, 
being further back "looks" identical as it relates to a point cloud, so the robot only has to drive a little further to complete the circle

\subsection*{Round Hallway --- With Landmarks}
Now we add landmarks to the surfaces of the walls to measure their impact

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RoundHallway/RoundHallway_landmark_MapImg.png}
        \caption{environment image.}
        \label{fig:round_lm_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RoundHallway/RoundHallway_Landmark_Traj.png}
        \caption{Trajectory comparison.}
        \label{fig:round_lm_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RoundHallway/RoundHallway_Landmark_Grid.png}
        \caption{Occupancy grid output.}
        \label{fig:round_lm_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/RoundHallway/RoundHallway__landmarkerror_overtime.png}}}
        \caption{Localisation error over time.}
        \label{fig:round_lm_error}
    \end{subfigure}

    \caption{Round Hallway (with landmarks) results.}
    \label{fig:round_lm_results}
\end{figure}

Once again, we are shown the importance of surface features for ICP performance. 
We see a near halving of the localization error, and on the trajectory, we see that ICP is much less 
far back compared to non-landmarks. 

While the mapping is still good, here we more clearly see the impact of ICP. Since there's landmarks and walls look different along the path, 
compared to no landmarks, a localization error will impact the mapping. We see this in the displacement and lack of some of the landmarks
from our occupancy grid.

\newpage

\subsection*{Crosses Map}
Now we start increasing the complexity of the Map. Here we have a map very symmetrical in nature, with clear but 
repeated landmarks. 

We take a more complex path through the map to better test our system and to get a complete mapping of the environment

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/GridWorld/GridWorld2_MapImg.png}
        \caption{environment image.}
        \label{fig:grid2_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/GridWorld/GridWorld_2_traj.png}
        \caption{Trajectory comparison.}
        \label{fig:grid2_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/GridWorld/GridWorld_2_grid.png}
        \caption{Occupancy grid output.}
        \label{fig:grid2_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.63\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth,height=5cm]{../images/Experiments/GridWorld/GridWorld_2_errorline.png}}}
        \caption{Localisation error over time.}
        \label{fig:grid2_error}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.45\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/GridWorld/GridWorld_1_grid.png}
        \caption{Occupancy grid mid-trajectory.}
        \label{fig:cross_map_warped_grid}
    \end{subfigure}

    \caption{Crosses Map results.}
    \label{fig:grid2_results}
\end{figure}

This map provided a big challenge for ICP due to its symmetry and winding path, visible in the noticeable divergence between the green ICP line and the pink pose-graph-optimised path.
During traversal the occupancy grid exhibited clear warping (Figure~\ref{fig:cross_map_warped_grid}); once a loop closure was detected and the pose graph optimised, the trajectory snapped into place and the warping was removed.
This is quantified by the substantial drop in localisation error from 0.12 to below 0.01, demonstrating the importance and strength of the pose graph optimisation step.


\subsection*{Ball Map}
This map is similar in it's simmetry to the crosses map, but has round objects.

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/BallMap/BallMap_MapImg.png}
        \caption{environment image.}
        \label{fig:ball_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/BallMap/BallWorld_traj.png}
        \caption{Trajectory comparison.}
        \label{fig:ball_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/BallMap/BallWorld_grid.png}
        \caption{Occupancy grid output.}
        \label{fig:ball_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/BallMap/BallMap_errorline.png}}}
        \caption{Localisation error over time.}
        \label{fig:ball_error}
    \end{subfigure}

    \caption{Ball Map results.}
    \label{fig:ball_results}
\end{figure}

We see that the results are quite similar to crosses map, which is to be expected given the similar layouts of the 
environments and paths taken. There is slightly less localization error, which could be from normal variance across runs, 
but could also be impacted by the smaller size of the landmarks in relation to the map, as well as the different surface normals. 

\newpage


\subsection*{Everything Round}
We combine round walls with round surfaces

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/UltraRound/UltraRound_MapImg.png}
        \caption{environment image.}
        \label{fig:ultra_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/UltraRound/UltraRound_traj.png}
        \caption{Trajectory comparison.}
        \label{fig:ultra_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/UltraRound/UltraRound_grid.png}
        \caption{Occupancy grid output.}
        \label{fig:ultra_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/UltraRound/UltraRound_errorlien.png}}}
        \caption{Localisation error over time.}
        \label{fig:everything_round_error}
    \end{subfigure}

    \caption{Everything Round results.}
    \label{fig:everything_round_results}
\end{figure}

The performance on this environment is surprisingly good, given our past issues with a round wall and highly symmetric environments.

Given the performance here, we can deduct that curved surfaces perform well quite well as landmarks.

\newpage
\subsection*{Regular Map}
This map was used during development. It is highly asymmetrical and quite random in nature

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RegularMap/RegularMap_MapImg.png}
        \caption{environment image.}
        \label{fig:regular_mapimg}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RegularMap/RegularMap_traj.png}
        \caption{Trajectory comparison.}
        \label{fig:regular_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/RegularMap/RegularMap_grid.png}
        \caption{Occupancy grid output.}
        \label{fig:regular_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/RegularMap/RegularMap_errorline.png}}}
        \caption{Localisation error over time.}
        \label{fig:regular_error}
    \end{subfigure}

    \caption{Regular Map results.}
    \label{fig:regular_results}
\end{figure}

The performance in both trajectory and mapping is excellent, as we might expect in a map with clear landmarks

\newpage
\subsection*{Apartment}
Fially, we test our system on a pre made Webots map, made for other scenarios. This serves as a "real world" test

\begin{figure}[H]
    \centering
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/Apartment/Apartment_MapImg.png}
        \caption{Environment Image.}
        \label{fig:apartment_mapimg}
    \end{subfigure}
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/Apartment/Apartment_traj.png}
        \caption{Trajectory comparison.}
        \label{fig:apartment_traj}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.30\textwidth}
        \centering
        \includegraphics[height=5cm]{../images/Experiments/Apartment/Apartment_grid.png}
        \caption{Occupancy grid output.}
        \label{fig:apartment_grid}
    \end{subfigure}

    \vspace{0.8em}

    \begin{subfigure}[t]{0.70\textwidth}
        \centering
        {\setlength{\fboxsep}{0pt}\setlength{\fboxrule}{0.5pt}%
        \fbox{\includegraphics[width=\textwidth]{../images/Experiments/Apartment/Apartment_errorLine.png}}}
        \caption{Localisation error over time.}
        \label{fig:apartment_error}
    \end{subfigure}

    \caption{Apartment results.}
    \label{fig:apartment_results}
\end{figure}

Once again, both trajectory and grid mapping are very accurate. This map shows us that our algorithm works in real
life environments.

\newpage

\section{Analysis}
These results show a SLAM algorithm that performs exceptionally well in a variety of environments. 
These experiments showed the impact of various environment factors on the systems's performance. 

\textbf{Symmetry} In the Crosses Map and the Ball Map we see that a highly symmetric map gives issues to ICP in localization. 
Both Apartment and Regular Maps have highly assymetrical environments, and this is seen in their performance.

\textbf{Landmarks} In both hallway maps, we saw an immediate and significant improvement in performance once landmarks were introduced. 
As we noted, ICP makes great use of landmarks to best calculate relative transformations. 

\textbf{Round Landmarks} Surprisingly, we saw that round landmarks did very well both in Ball Map and in Everythin Round. 
I hypothesize that the contrast of the curved landmarks and the walls behind them are more identifiable to the ICP system 
than landmarks made of straight surfaces. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{../images/StraightVsCurvedLandmark.jpeg}
    \caption{Straight vs Curved Landmark}
    \small{Red surface projections on white surfaces from lidar rays. Straight landmark looks 
    similar to wall behind, while round landmark has distinctive geometry compared to the wall behind}
    \label{fig:straight_v_curved_landmarks}
\end{figure}


\subsection{Wave-like Error over Time}
We may notice that in multiple maps: HallwayMap, CrossesMap, BallMap, EverthingMap, and RegularMap, 
there is a distinctive "wave" like behaviour in error over time. After analyzing the trajectories taken, as well as the 
error over time as the trajectory is being taken live, I notice that these bumps/increases in error correspond to moments of major rotation 
during the trajectory. This can also be seen in trajectory lines: where the robot is undergoing a trajectory
with significant arc, the green ICP line is more evident, showing us a bigger mismatch with ground truth. 
\newline
These results can be attributed to ICP's weakness when undergoing rotation, since once the rotation stops and a more
straight trajectory is resumed, error lowers significantly. 

We conclude that the main weakness in the system is when undergoing rotatoin, and it's recognition and filtering of landmarks and surfaces. 

Overall, we see that we have a strong SLAM system, in which we can see all parts of the system performing well. 
ICP is able to map it's trajectory, we then combine this data with point clouds to update our occupancy grid, and once 
loop closures are found, our Pose Graph Optimization is able to consistently reduce error, correct our trajectory, and snap our map into place. 

\section{Future Work}

This project created a working SLAM system, but one that could be further improved to work with larger and more complex environments.
It is also one that shows the working structure of a SLAM system, real world systems build on this architecture and add complex processing 
for features such as object detection and tracking, that allow them to be used in real world systems such as in autonomously navigating vehicles. 
Several directions could improve the core system:

\begin{itemize}
    \item \textbf{Point-to-plane correspondence matching.}
          Minimizing the distance from a source point to the plane containing its corresponding target
          point is more robust to noise than point-to-point matching, particularly in planar
          environments. 

    \item \textbf{Sensor fusion with an Extended Kalman Filter.}
          An EKF could intelligently weight contributions from odometry and lidar, especially during
          rotations where ICP alone performs poorly.

    \item \textbf{Scaling to larger environments.}
          All parameters and data structures are tuned for a small, controlled arena.
          Adapting to larger maps would require algorithmic improvements to loop closure candidate
          search, correspondence matching, and optimization.
\end{itemize}

% ============================================================
%  REFERENCES
% ============================================================
\renewcommand{\bibname}{References}
\addcontentsline{toc}{chapter}{References}
\begin{thebibliography}{10}

\bibitem{dunbar2005swarm}
T.~W.~Dunbar and J.~Esposito,
``Artificial Potential Field Controllers for Robust Communications in a Network of Swarm Robots,''
in \textit{Proc.\ 37th Southeastern Symposium on System Theory (SSST)}, 2005, pp.\ 401--405.
DOI: \href{https://doi.org/10.1109/SSST.2005.1460945}{10.1109/SSST.2005.1460945}

\bibitem{bresenham_mbedded}
G.~Hunter,
``Bresenham's Line Algorithm,''
\textit{mbedded.ninja}, Jan.\ 30, 2019.
[Online]. Available: \url{https://blog.mbedded.ninja/programming/algorithms-and-data-structures/bresenhams-line-algorithm/}

\bibitem{mishra2020kinematics}
D.~Mishra, R.~S.~Yadav, and K.~K.~Agrawal,
``Kinematic Modelling and Emulation of Robot for Traversing over the Pipeline in the Refinery,''
\textit{Microsystem Technologies}, vol.\ 26, no.\ 3, pp.\ 1011--1020, 2020.
DOI: \href{https://doi.org/10.1007/s00542-019-04615-9}{10.1007/s00542-019-04615-9}

\bibitem{rodriguez2010grid}
D.~Rodr\'iguez-Losada, P.~de~la~Puente, A.~Valero-Gomez, P.~San~Segundo, and M.~Hernando,
``Fast Processing of Grid Maps using Graphical Multiprocessors,''
2010.
[Online]. Available: \url{https://www.researchgate.net/figure/Bresenham-line-algorithm-for-a-laser-beam-Note-that-the-occupied-cells-include-those_fig2_267690266}

\bibitem{pose_graph_thesis}
Max Pfingsthorn
``Generalized Simultaneous Localization and Mapping (SLAM) on Graphs with Multimodal Probabilities and Hyperedges,''
Ph.D.\ thesis, Mar.\ 2014.
Supervisor: A.~Birk.
DOI: \href{https://doi.org/10.13140/RG.2.1.4662.3600}{10.13140/RG.2.1.4662.3600}

\bibitem{gtsam}
GTSAM Development Team,
``\texttt{gtsam::NonlinearOptimizer} Class Reference,''
\textit{GTSAM 4.0.0 API Documentation}, 2019.
[Online]. Available: \url{https://gtsam.org/doxygen/4.0.0/a03807.html}

\bibitem{stachniss2020posegraph}
C.~Stachniss,
``Graph-Based SLAM Using Pose Graphs,''
\textit{YouTube}, Apr.\ 3, 2020.
[Online]. Available: \url{https://www.youtube.com/watch?v=uHbRKvD8TWg}

\bibitem{stachniss2021icp}
C.~Stachniss,
``Point Cloud Registration with Unknown Data Associations Using the Iterative Closest Point (ICP) Algorithm'' (3-part lecture series),
\textit{YouTube}, Mar.\ 21, 2021.
[Online]. Available: \url{https://www.youtube.com/watch?v=dhzLQfDBx2Q}

\bibitem{stachniss2020occgrid}
C.~Stachniss and N.~Chebrolu,
``Occupancy Grid Maps,''
\textit{Mobile Sensing and Robotics}, YouTube, Aug.\ 27, 2020.
[Online]. Available: \url{https://www.youtube.com/watch?v=v-Rm9TUG9LA}

\bibitem{stachniss2020leastsquares}
C.~Stachniss,
``An Informal Introduction to Least Squares,''
\textit{YouTube}, Apr.\ 3, 2020.
[Online]. Available: \url{https://www.youtube.com/watch?v=r2cyMQ5NB1o}

\end{thebibliography}




% ============================================================
%  APPENDIX
% ============================================================
\appendix

\chapter{Source Code}
\label{app:code}

Source code for this project is submitted in electronic format alongside this report.
\newline
It can be found at \href{https://github.com/MarioPardo/slam-from-scratch}{Github Repository}
\newline
https://github.com/MarioPardo/slam-from-scratch

\vspace{1cm}


\end{document}
